export enum ROUTES {
  ERROR = "*",
  HOME = "/home",
  OVERVIEW = "/",
  REDIRECT = "/*",
  INBOX = "/inbox",
  LOGIN = "/login",
  DETAIL = "/detail/",
  SIGNUP = "/signup",
  PROFILE = "/profile",
  MESSAGE = "/message",
  FAVORITES = "/favorites",
  DETAIL_PARAMS = "/detail/:id",
}
