export enum ROUTES {
  ERROR = "*",
  HOME = "/Home",
  REDIRECT = "/*",
  INBOX = "/inbox",
  LOGIN = "/login",
  SIGN_UP = "/signup",
  PROFILE = "/profile",
  OVERVIEW = "/overview",
  FAVORITES = "/favorites",
}
