export enum ROUTES {
  ERROR = "*",
  HOME = "/home",
  OVERVIEW = "/",
  REDIRECT = "/*",
  INBOX = "/inbox",
  LOGIN = "/login",
  DETAIL = "/detail/",
  SIGN_UP = "/signup",
  PROFILE = "/profile",
  FAVORITES = "/favorites",
  DETAIL_PARAMS = "/detail/:id",
}
